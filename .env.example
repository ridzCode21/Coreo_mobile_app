# Copy to .env and fill in for local development. EXPO_PUBLIC_* vars are inlined at build time
# and are NOT secret — never put real secrets behind an EXPO_PUBLIC_ prefix.
EXPO_PUBLIC_API_URL=https://api.coreo.dev
